generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "./main_database.db"
}

model Task {
  id            Int      @id @default(autoincrement())
  title         String
  status        String
  description   String?
}
